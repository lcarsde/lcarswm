plugins {
    id 'org.jetbrains.kotlin.multiplatform' version '1.3.50'
}

group 'de.atennert'
version '0.0.2'

repositories {
    mavenCentral()
}

kotlin {
    linuxX64("native") {
        compilations.main.cinterops {
            xlib
        }
        binaries {
            executable()
        }
    }
}

wrapper {
    gradleVersion = "5.2.1"
    distributionType = "ALL"
}
