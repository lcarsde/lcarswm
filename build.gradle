plugins {
    id 'org.jetbrains.kotlin.multiplatform' version '1.3.21'
}

group 'de.atennert'
version '0.0.1'

repositories {
    mavenCentral()
}

kotlin {
    linuxX64("native") {
        compilations.main.cinterops {
            xcb
        }
        binaries {
            executable()
        }
    }
}

wrapper {
    gradleVersion = "5.2.1"
    distributionType = "ALL"
}