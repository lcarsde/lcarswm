os: linux
dist: xenial

language: java
jdk: openjdk8

notifications:
  email:
    on_success: never
    on_failure: always

before_install:
- sudo apt-get update
- sudo apt-get install -y openjdk-8-jdk-headless libx11-dev libxpm-dev libxrandr-dev libpango1.0-dev libicu-dev libxml2-dev
